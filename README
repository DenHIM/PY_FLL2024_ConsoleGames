# Commands
- Save all requirements in requirements.txt file
```
pip freeze > requirements.txt
```
- Install all requirements
```
pip install -r /path/to/requirements.txt
```

#pygbag
```
python -m pygbag game_pygame_test

python -m pygbag --build game_pygame_test
cd "/content/Games/game_pygame_test/"
python -m http.server 8000
```